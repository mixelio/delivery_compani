"use strict";

$(document).ready(function () {
  $(".slider").slick({
    arrows: true,
    slidesToShow: 1,
    infinite: true,
    speed: 500,
    autoplay: true,
    autoplaySpeed: 6000,
  });
});
